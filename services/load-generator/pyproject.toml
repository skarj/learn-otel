[project]
name = "load-generator"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "otel-common",
]

[tool.uv.sources]
otel-common = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["load_generator"]
